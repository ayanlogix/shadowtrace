{
  "name": "@app/shadowtrace-contract",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compact": "node ../scripts/compact-compile.mjs",
    "build": "npm run compact && npm run sync:assets",
    "build:contract": "npm run compact && npm run sync:assets",
    "sync:assets": "node ../scripts/sync-zk.mjs"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "0.16.0",
    "@midnight-ntwrk/ledger-v8": "^8.0.3"
  }
}
